{{nav-bar}}
<h1>Contact Page</h1>

<p class="well well-sm">If you have any question or feedback please leave a message.</p>

<div class="row">
    <div class="col-md-6">


        <div class="form-group has-feedback {{if model.isMessageEnoughLong 'has-success'}}">
            <label>Your message*:</label>
          {{textarea class="form-control" placeholder="Your message. (At least 5 characters.)" rows="5" value=model.message}}
          {{#if model.isMessageEnoughLong}}<span class="glyphicon glyphicon-ok form-control-feedback"></span>{{/if}}
        </div>
        <button style="color:white;background-color: #63AE81;border-radius: 12px;" {{action 'sendMessage' model}} disabled={{model.isNotValid}}>Send</button>

    </div>
</div>
